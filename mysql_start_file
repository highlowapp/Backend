#This is run only once. Create a database with four columns in the high_low_db table

CREATE DATABASE high_low_db;

USE high_low_db;

CREATE TABLE high_low_db(
first_name TEXT,
last_name TEXT,
email TEXT,
password TEXT);
